<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Édition du commentaire</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 500px;
      margin: 50px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      display: flex; /* Ajout de la propriété flex pour utiliser la disposition flexbox */
    }

    .image-container {
      flex: 0 0 100px; /* Largeur fixe de l'image */
      margin-right: 20px; /* Marge à droite pour l'espace */
    }

    .image-container img {
      width: 100%; /* Assurer que l'image occupe toute la largeur du conteneur */
      border-radius: 8px; /* Ajouter une bordure arrondie à l'image */
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
    }

    #error_explanation {
      background-color: #f8d7da;
      color: #721c24;
      padding: 10px;
      border-radius: 5px;
      margin-bottom: 20px;
    }

    #error_explanation h2 {
      margin-top: 0;
    }

    #error_explanation ul {
      list-style-type: none;
      padding: 0;
    }

    #error_explanation li {
      margin-bottom: 5px;
    }

    .field {
      margin-bottom: 20px;
    }

    .field label {
      display: block;
      font-weight: bold;
      margin-bottom: 5px;
    }

    .field textarea {
      width: 100%;
      height: 100px;
      padding: 10px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .actions {
      text-align: center;
    }

    .actions input[type="submit"] {
      padding: 10px 20px;
      font-size: 16px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .actions input[type="submit"]:hover {
      background-color: #0056b3;
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 20px;
      text-decoration: none;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="image-container">
      <img src="https://i.pinimg.com/564x/30/b2/25/30b22589474c3b0200aecbde2f78407e.jpg" alt="Daisy Duck">
    </div>
    <div>
      <h1>Édition du commentaire</h1>

      <% if @comment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

          <ul>
            <% @comment.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form_with(model: [@gossip, @comment], url: gossip_comment_path(@gossip, @comment), method: :patch, local: true) do |form| %>
        <div class="field">
          <%= form.label :text %>
          <%= form.text_area :text %>
        </div>

        <div class="actions">
          <%= form.submit "Update Comment" %>
        </div>
      <% end %>

      <%= link_to 'Back', gossip_path(@gossip), class: 'back-link' %>
    </div>
  </div>
</body>
</html>
